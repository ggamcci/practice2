# practice2

### Description
This project implements a Flask REST API with:
- 8 endpoints (2 each for GET, POST, PUT, DELETE)
- Middleware for request logging
- Standardized JSON response format
- Various HTTP response codes (2xx, 4xx, 5xx)

### Run Instructions
```bash
pip install flask
python app.py
